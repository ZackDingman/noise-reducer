// config.js — default public-demo config (no keys needed for the iframes)
// For API calls, paste your private endpoints & tokens here.

window.CONFIG = {
  hfA: { url: "", token: "" }, // DeepFilterNet endpoint URL + token (optional)
  hfB: { url: "", token: "" }, // MetricGAN+/FullSubNet endpoint URL + token (optional)
  replicate: { version: "", token: "" } // Demucs version hash + Replicate token (optional)
};
